datasources:
  default:
    url: ${JDBC_URL:`jdbc:postgresql://localhost/postgres`}
    username: ${JDBC_USER:`postgres`}
    password: ${JDBC_PASSWORD:`postgres`}
    schema: kick
    maximumPoolSize: 1
logger:
  levels:
    io.github.opendonationassistant: INFO
rabbitmq:
  host: ${RABBITMQ_HOST:`localhost`}
